@model IEnumerable<GameSpace.Models.UserSignInStat>

@{
    ViewData["Title"] = "Sign In History";
}

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h3>Sign In History</h3>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>Sign In Time</th>
                                <th>Points Earned</th>
                                <th>Status</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.SignTime.ToString("yyyy-MM-dd HH:mm:ss")</td>
                                    <td>@item.Point</td>
                                    <td>
                                        @if (item.Point > 0)
                                        {
                                            <span class="badge bg-success">Success</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-warning">No Points</span>
                                        }
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>